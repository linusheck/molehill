// ??
pomdp

observables
	s
endobservables

const int N = 100;

module clk
    s : [-1..N] init 0;

	[right] s<N & s>=0 -> 1 : (s'=s+1);
	[up]    s<N & s>=0 -> 0.9 : (s'=s+1) + 0.1 : (s'=-1);
endmodule

label "goal" = s=N;
label "bad" = s=-1;
